// Decide se vai ser maçã ou banana
tipo = choose(0, 1);
if (tipo == 0) sprite_index = spr_maca;
else sprite_index = spr_banana; // Lembre-se de usar o nome exato do seu sprite da banana!

tempo_vida = 300; // Fica no chão por 5 segundos e some se não pegar